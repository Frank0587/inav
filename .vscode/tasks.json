{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "iNav: build SPEEDYBEEF405WING (in Docker)",
            "type": "shell",
            "command": "docker run --rm -it -u root -v v:/git/inav:/src inav-build SPEEDYBEEF405WING",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },
        {
            "label": "iNav: delete cache (when error occures)",
            "type": "shell",
            "command": "rd /s /q .\\CMakeFiles",
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },
        {
            "label": "iNav: create docker image 'inav-build'",
            "type": "shell",
            "command": "docker build --build-arg USER_ID=1000 --build-arg GROUP_ID=1000 -t inav-build .",
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ]
}