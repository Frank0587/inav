{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker run SPEEDYBEEF405WING",
            "type": "shell",
            "command": "docker run -it -u root -v v:/git/inav:/src inav-build SPEEDYBEEF405WING",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },
        {
            "label": "setup docker iNav container",
            "type": "shell",
            "command": "docker build --build-arg USER_ID=1000 --build-arg GROUP_ID=1000 -t inav-build .",
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ]
}